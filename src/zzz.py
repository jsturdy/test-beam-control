# System imports
from system import *

# Create window
window = Window("Write a GLIB or OptoHybrid register")

window.drawColor()

# Wait before quiting
window.waitForQuit()

# Close window
window.close()
